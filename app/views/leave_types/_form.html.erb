
<%= simple_form_for(@leave_type) do |f| %>
  <% if @leave_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leave_type.errors.count, "error") %> prohibited this leave type from being saved:</h2>

        <% @leave_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>

    </div>
  <% end %>
  <div class="container">
    <div class="row">
      <div class="column">
        <label>Leave Type</label>
        <%= f.input :name,  label:false, required: true,:prompt => "Select Leave Type" %>
      </div>
      <div class="column">
        <label>Count</label>
        <%= f.input :count,label: false,required: true %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="column">
      <%= f.button :submit %>
    </div>
    <div class="column">
      <%= link_to 'Back', leave_types_path %>
    </div>

  </div>
<% end %>
